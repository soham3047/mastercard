# Person C — Adversarial Loop + Cross-Bank — Progress Log

Append one `## SYNOPSIS — Person C Stage N: [name]` block per completed
stage (format in root README). Next stage: **C0 — Scaffold + stub loop**
(or C3 first if blocked waiting on A/B).
